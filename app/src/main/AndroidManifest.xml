<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devsyafii.crudsqlite">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.account.AboutMeActivity" />
        <activity android:name=".controller.Create.CreateActivity" />
        <activity android:name=".controller.Delete.DeleteActivity" />
        <activity android:name=".controller.Read.ReadActivity" />
        <activity android:name=".controller.Update.UpdateActivity" />
        <activity android:name=".controller.Home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>